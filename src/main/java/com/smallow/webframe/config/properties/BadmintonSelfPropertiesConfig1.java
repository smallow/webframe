package com.smallow.webframe.config.properties;

import lombok.Data;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.PropertySource;
import org.springframework.stereotype.Component;

/**
 * 自定义properties文件测试
 */
/*@Component
@ConfigurationProperties(prefix = "bd")
@PropertySource(value = "classpath:config/badminton.properties", encoding = "UTF-8")
@Data
@Deprecated
public class BadmintonSelfPropertiesConfig1 {

    private String city;
    private Integer interval;
    private Boolean kaptcha;//是否开启登录验证码
    private String fileUploadPath;


    private Boolean springSessionOpen = false;

    private Integer sessionInvalidateTime = 30 * 60;  //session 失效时间（默认为30分钟 单位：秒）

    private Integer sessionValidationInterval = 15 * 60;  //session 验证失效时间（默认为15分钟 单位：秒）
}*/
